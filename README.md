# force-login-view
Force Login to View Page is a WordPress plugin that restricts site access to logged-in users only. It offers flexible exclusions for pages, templates, roles, users, IPs, and post types, plus bypass tokens, custom login redirects, and a clean, easy-to-use admin interface.
